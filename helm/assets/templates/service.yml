apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-app
  labels:
    app: {{ .Chart.Name }}
    tier: frontend
spec:
  type: NodePort
  ports:
  - port: 8080
  selector:
    app: {{ .Chart.Name }}
    tier: frontend

